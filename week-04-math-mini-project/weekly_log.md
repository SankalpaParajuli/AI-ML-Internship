# Weekly Log — Week 4

**What I did:** Built intuition for the linear algebra, probability, and calculus ideas behind ML
(vectors, dot products, matrix multiplication, conditional probability, distributions, and gradient
descent), then applied a full independent EDA workflow (clean → explore → visualize → summarize) on
the Palmer Penguins dataset as this week's mini project.

**What worked:** Implementing gradient descent from scratch on a simple 1D function made the
"training = walking downhill" idea click much better than reading the formula alone. Choosing a
dataset with real missing values (penguins) made the cleaning step feel genuinely necessary rather
than a formality.

**What didn't:** Conditional probability intuition took a couple of re-reads with a concrete example
(deck of cards) before it fully made sense — plotting distributions side by side helped more than the
formulas did.

**Next:** Moving into Week 5 — supervised learning with Linear and Logistic Regression using
scikit-learn.
